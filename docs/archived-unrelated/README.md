# Archived — unrelated files

Files in this folder were found in the repo but do not belong to Propellect / property-ai.
They are kept here (instead of deleted) until confirmed safe to remove.

## colesCurrentCategoryExtractor.js

Appears to belong to an older grocery / per-100g price-comparison project
(it extracts product category data from Coles pages). It has no imports from,
or references to, any property-ai code. Moved out of `tools/` during
Warehouse Sprint 0 cleanup (July 2026). Safe to delete once confirmed it is
not needed elsewhere.
