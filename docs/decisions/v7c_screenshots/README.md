# V7C curated UAT screenshots

Committed, curated desktop/mobile PNGs from the Playwright UAT (`npm run uat:v7c`), named `<journey>.<desktop|mobile>.png`.

Raw Playwright traces, videos, HTML report and the auth storage state live under `uat/v7c/.artifacts/` and `uat/v7c/.auth/` and are **gitignored** (never committed). `uat-result.json` here is the machine-readable summary.
